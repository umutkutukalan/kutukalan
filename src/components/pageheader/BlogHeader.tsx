const BlogHeader = () => {
  return (
    <div className="w-full flex items-center justify-between border-b pb-2">
      <div>
        <h1 className="text-[clamp(1rem,1.6vw,2rem)] 3xl:text-[clamp(1.5rem,1.8vw,2rem)] 4xl:text-[3rem]">
          BLOGLAR
        </h1>
      </div>
    </div>
  );
};

export default BlogHeader;
